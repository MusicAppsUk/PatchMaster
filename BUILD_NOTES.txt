PatchMaster v21.11 — STABLE REBUILD — 13 Sep 2026

Recovery strategy:
- Rebuilt from v21.5 Evidence Rescue, the last hardware-confirmed musical/transport baseline (Vienna passed on the MODX M8).
- Did NOT inherit v21.6–v21.10 cumulative experimental patches.
- Replaced only catalogue discovery with a broader title/artist/collaboration search (up to 40 results).
- Fixed the latent hasKey runtime exception in the build path.
- Added a zero-parts stop: PatchMaster must not jump to SEND with an empty blueprint.
- A one-Part researched rig is full keyboard range A0–C8 to avoid apparent silence from role splits.
- Yamaha PREP/PLAYABLE transaction and PM_ALLOW remain from v21.5.
- Bulk-memory writes remain blocked.

Important: physical MODX M validation is still required. This build is deliberately a recovery baseline, not a claim of universal song accuracy.
